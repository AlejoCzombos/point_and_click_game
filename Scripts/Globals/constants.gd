class_name Constants

## ---------------- Enums -----------------

enum TransitionType {
	FADE,
	SLIDE,
	SLIDE_BLACK,
}

enum Direction {
	LEFT,
	RIGHT,
}

enum HotspotType {
	SCENE,
	LEVEL
}

## ---------------- Consts -----------------

const load_duration: float = 0.25
const slide_duration: float = 0.4
